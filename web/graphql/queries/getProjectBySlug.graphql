query ProjectBySlug($slug: String!) {
  project:project(where: { slug: $slug }){
    name
    images {
      name
      altText
      image {
        publicUrl
      }
    }
    description {
      document
    }
  }
}